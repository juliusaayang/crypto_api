import 'package:go_router/go_router.dart';

extension GoRouterStateExtension on GoRouterState {
  // bool atPathAvailableWithoutLogin(GoRouter goRouter) {
  //   final availableWithoutLogin = Routes.availableWithoutLogin.map(goRouter.namedLocation).any((it) {
  //     return it.contains(matchedLocation);
  //   });
  //   return availableWithoutLogin;
  // }
  //
  // Arguments toArguments() {
  //   return Arguments(
  //     pathParameters: pathParameters,
  //     extra: extra,
  //   );
  // }
}
